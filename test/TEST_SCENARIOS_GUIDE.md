# Test Scenarios Guide

This guide describes the types of tests you can run against the Wazuh infrastructure.

---

## Overview

All tests use the **same baseline infrastructure** (Wazuh server + agent in terraform/). Test-specific requirements (additional security groups, resources, configuration) are **generated by agents** in `test/terraform/` during the test and **deleted during cleanup**.

This approach keeps the baseline clean and lets each test define exactly what it needs.

---

## Available Test Types

### 1. Fresh Deployment (Baseline)

**Purpose**: Verify Wazuh server and agent deploy and communicate correctly.

**What deploys**:
- Wazuh server (manager + indexer + dashboard)
- Wazuh agent (auto-enrolled)
- Basic security groups

**When to use**:
- Testing new Wazuh versions
- Validating infrastructure changes
- Establishing baseline before testing features

**Typical duration**: 45 minutes  
**Cost**: ~$0.21

**Verification**:
```bash
ssh -i terraform/wazuh-test-key.pem ubuntu@<server_dns>
sudo systemctl status wazuh-manager wazuh-indexer wazuh-dashboard
sudo /var/ossec/bin/agent_control -l
```

---

### 2. Blog Post Testing (EOL Detection)

**Purpose**: Validate implementation of a blog post (e.g., EOL detection).

**How it works**:
1. Agent reads blog post/documentation
2. Agent generates required infrastructure in `test/terraform/`
   - Example: Security groups for external API access
   - Example: Custom rules file deployment
   - Example: Additional Python scripts
3. Deploy with `terraform apply`
4. Execute blog post procedures
5. Document findings
6. Cleanup: `terraform destroy && rm -rf test/terraform/`

**When to use**:
- Testing blog post implementations
- Validating custom integrations
- Verifying documented procedures

**Typical duration**: 60 minutes  
**Cost**: ~$0.32

**What agent generates**:
```bash
cat > test/terraform/generated-test.tf << 'EOF'
# Generated from: [blog post analyzed]
# Purpose: [test objective]

resource "aws_security_group_rule" "external_api" {
  # Allow external API calls needed by blog post
  ...
}

resource "local_file" "custom_script" {
  filename = "/tmp/custom-integration.py"
  content  = file("${path.module}/../requirements/custom-script.py")
}
EOF
```

---

### 3. Documentation Testing

**Purpose**: Validate official Wazuh documentation against live infrastructure.

**How it works**:
1. Agent reads Wazuh official documentation
2. Agent extracts documented procedures
3. Agent generates infrastructure to match doc requirements
4. Deploy and execute procedures from docs
5. Verify actual behavior matches documentation
6. Document discrepancies

**When to use**:
- Testing new Wazuh versions' documented procedures
- Identifying outdated documentation
- Validating API procedures
- Testing dashboard features

**Typical duration**: 60-120 minutes  
**Cost**: ~$0.42-0.84

**Test template**: See [DOCUMENTATION_TEST_TEMPLATE.md](DOCUMENTATION_TEST_TEMPLATE.md)

---

### 4. Upgrade Testing (4.14.6 → 5.0.0)

**Purpose**: Test the upgrade path from 4.14.6 to 5.0.0.

**How it works**:
1. Deploy 4.14.6 baseline
2. Generate upgrade-specific code in `test/terraform/`
   - Backup procedures
   - Upgrade verification checks
3. Execute documented upgrade procedure
4. Verify 5.0.0 operational
5. Test agent re-enrollment
6. Document upgrade findings
7. Test rollback if needed

**When to use**:
- Planning production upgrades
- Testing migration procedures
- Validating backward compatibility
- Verifying data preservation

**Typical duration**: 120 minutes  
**Cost**: ~$0.44

**Test template**: See [UPGRADE_TEST_TEMPLATE.md](UPGRADE_TEST_TEMPLATE.md)

---

### 5. Dashboard Access Testing

**Purpose**: Validate Wazuh dashboard UI/UX and features.

**How it works**:
1. Deploy baseline
2. Generate test code for:
   - Test data creation
   - Feature enablement settings
3. Access dashboard and test features
4. Capture screenshots/findings
5. Document UI/UX observations

**When to use**:
- Testing dashboard feature updates
- Usability testing
- Documenting UI behavior
- Screenshot capture for docs

**Typical duration**: 45 minutes  
**Cost**: ~$0.21

---

### 6. Agent Enrollment Testing

**Purpose**: Test agent enrollment protocols and communication.

**How it works**:
1. Deploy baseline (server + one agent)
2. Generate code for:
   - Additional agent deployments
   - Pre-auth key generation
   - Enrollment verification scripts
3. Test enrollment flows
4. Verify agent communication
5. Test agent updates/reconfiguration

**When to use**:
- Testing agent enrollment protocol changes
- Validating pre-auth key workflows
- Testing agent discovery
- Agent scaling tests

**Typical duration**: 45 minutes  
**Cost**: ~$0.21

---

### 7. Custom Scenarios

Any test not listed above can be created by agents:

1. Agent reads whatever documentation/blog post/feature description
2. Agent analyzes infrastructure requirements
3. Agent generates `test/terraform/` code with those requirements
4. Deploy, test, document, cleanup

This is the **design principle**: baseline stays clean, test needs are generated.

---

## General Test Workflow

All tests follow this pattern:

```
1. READ
   Agent reads documentation/blog post/requirements
   
2. ANALYZE
   Agent extracts infrastructure needs
   
3. GENERATE
   Agent creates test/terraform/generated-*.tf files
   
4. DEPLOY
   terraform apply (uses baseline + generated code)
   
5. TEST
   Execute documented procedures
   Follow TEST_SCENARIOS_GUIDE.md for the specific type
   
6. DOCUMENT
   Save findings to results/
   
7. CLEANUP
   terraform destroy
   rm -rf test/terraform/ results/* test/requirements/*
   (sweep for stray test scripts elsewhere too)
   
8. VERIFY
   Repository is identical to baseline
```

---

## Cost Estimation

| Scenario | Duration | Cost | Notes |
|----------|----------|------|-------|
| Fresh deployment | 45 min | $0.21 | Baseline only |
| Blog post | 60 min | $0.32 | +extra resources |
| Documentation | 60-120 min | $0.42-0.84 | Comprehensive testing |
| Upgrade | 120 min | $0.44 | Includes backup/restore |
| Dashboard | 45 min | $0.21 | UI testing only |
| Agent enrollment | 45 min | $0.21 | Protocol testing |
| Custom | Variable | Variable | Depends on test |

All assume default 4-hour TTL. Extend TTL in terraform.tfvars if needed.

---

## Verification Rules (R1-R4)

**R1: Never trust status, always verify with command**
- ❌ Don't: "Service is running" (assumed)
- ✅ DO: `sudo systemctl is-active wazuh-manager` → `active`
- ✅ DO: `curl https://localhost` → HTTP 302/200

**R2: Verify URLs before piping to shell**
- ❌ Don't: `curl -s URL | bash`
- ✅ DO: Check HTTP 200 first, then run

**R3: Review writing quality against the Wazuh style guide**
- Authoritative source: `test/Language and formatting style guide for technical writing _ Wazuh.md`.
  Check documents against this file directly — it is the standard, not a
  generic grammar checklist. If the guide and any other doc in this repo
  disagree on a writing rule, the style guide wins.
- High-value, mechanical checks worth running every time (non-exhaustive —
  read the guide for the rest):
  - Banned words: *could/should/would/may*, *etc./i.e./e.g.*, *please* (in instructions)
  - *can* vs. *might* vs. *may* — *can* for user actions, *might* for uncertain
    outcomes, never *may*
  - Sentence case for headings and titles, not Title Case (except marketing copy)
  - Oxford/serial comma in lists of three or more
  - No gender-specific pronouns (*he/she*) — use *you*, *they*, or *the user*
  - No possessive of product/company names ("the Wazuh agent", not "Wazuh's agent")
  - Deprecated terminology to flag wherever it appears, especially in rule or
    decoder descriptions: *OpenSCAP, OpenSearch, Kibana, ElasticSearch*
  - Custom Wazuh rule IDs fall in the 100000–120000 range
- Flag unclear instructions, missing prerequisites, or confusing steps
- Report writing issues separately from functional issues

**R4: Review document structure and automate what you can**
- Check heading hierarchy matches actual content — flag runs of near-empty
  subheadings (a list masquerading as sections) and wrong nesting
- For any 3+ step manual procedure touching files/config, ask whether it
  could be one script — and if so, write the script, don't just suggest it
- A script replacing a "go check the UI" step should verify its own result
  with a command (ties back to R1)
- Report structural/automation findings separately from writing issues

All test templates enforce these rules.

---

## Related Documentation

- [DOCUMENTATION_TEST_TEMPLATE.md](DOCUMENTATION_TEST_TEMPLATE.md) — Framework for testing docs
- [UPGRADE_TEST_TEMPLATE.md](UPGRADE_TEST_TEMPLATE.md) — Framework for testing upgrades
- [test/deployments/](deployments/) — Version-specific runbooks
- [TTL_AND_AUTO_TERMINATION.md](TTL_AND_AUTO_TERMINATION.md) — Cost control details
- `test/Language and formatting style guide for technical writing _ Wazuh.md` — Authoritative writing style guide (R3)
- [agent/AGENT_HANDOFF.md](../agent/AGENT_HANDOFF.md) — Agent onboarding

---

## Test Result Output Formats

All tests must produce results in **PDF and HTML** formats containing:

### PDF Report Format
- **Executive Summary** — Overall pass/fail, key metrics
- **Technical Review** — Step-by-step verification with actual vs expected outcomes
- **Code Review** — Validation of all code examples/commands
- **Writing Quality Review** — Spelling, grammar, formatting, clarity
- **Issues Summary** — Severity-ranked findings with evidence
- **Appendix** — Full command outputs, screenshots, timestamps

### HTML Report Format
- **Interactive Dashboard** — Status indicators, charts, collapsible sections
- **Timeline View** — Sequential steps with duration and status
- **Detailed Findings** — Each finding with location, example, and recommendation
- **Comparison Tables** — Pre/post states, version differences
- **Code Highlighting** — All code examples with syntax highlighting
- **Navigation** — Table of contents with jump links

### What Must Be Included in Both Formats

**Technical Elements**:
- ✅ Which procedures succeeded vs failed
- ✅ Actual command output vs expected
- ✅ Verification commands and results
- ✅ Service status checks (with `systemctl is-active`, not assumptions)
- ✅ URL validations (HTTP status codes)
- ✅ Configuration file validations
- ✅ Data integrity checks

**Writing Quality Elements** (checked against `test/Language and formatting
style guide for technical writing _ Wazuh.md`, not a generic checklist):
- ✅ Spelling and grammar issues (with exact location)
- ✅ Style guide violations (banned words, voice, capitalization, deprecated terms — cite the guide section)
- ✅ Code formatting problems (with examples)
- ✅ Consistency issues (with all instances listed)
- ✅ Clarity issues (with explanation of confusion)
- ✅ Missing prerequisites or expected outcomes
- ✅ Recommendations for improvement

**Severity Classification**:
- 🔴 **Critical** — Blocks understanding or breaks functionality
- 🟡 **Important** — Confusing or causes rework
- 🟢 **Minor** — Polish/style issues

---

**Last Updated**: 2026-07-29  
**System**: Baseline + generated test code  
**Output**: PDF + HTML reports with technical and writing reviews  
**Status**: All scenarios ready for agents
